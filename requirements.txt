# Upper bounds pin the next major version so a `pip install -U` can't
# silently pull a breaking release into a working deployment. Bump the
# ceiling deliberately after verifying the new major locally.
# Floor at 1.37: st.fragment (stable) is required by the live-progress
# watcher in pages/3 (non-blocking auto-refresh). Older versions only had
# st.experimental_fragment.
streamlit>=1.37.0,<2.0.0
# ⚠️ 传递依赖钉死(2026-08-05 生产事故):starlette 1.4.0 给
# GZipResponder.__init__ 新增了必填的 keyword-only 参数 thread_minimum_size,
# 而 streamlit(实测 1.61.0,声明的约束是 starlette<2,>=0.46 —— 拦不住)的
# _MediaAwareGZipResponder 仍按旧签名调用。后果:服务器对【所有】请求 500
# (TypeError: missing 'thread_minimum_size'),健康检查不过,应用整个起不来,
# app.py 根本没被执行到。Streamlit Cloud 每次重建容器都全新解析依赖,所以
# 这类"上游昨晚发新版"的破坏会在任意一次重启时突然出现。
# 下限 0.49.1 来自 sse-starlette 3.4.8(mcp 的依赖)的要求。
# 等 streamlit 适配 starlette 1.4 后再放开上限。
starlette>=0.49.1,<1.4.0
anthropic[vertex]>=0.25.0,<1.0.0
# google-auth 仍然需要:anthropic[vertex] 的 Vertex 凭证路径靠它。
# 主链路(Kimi/DeepSeek)用不到,但 secrets 里配了 GCP_PROJECT_ID 的老部署
# 还能继续跑 Claude on Vertex。
google-auth>=2.20.0,<3.0.0
# v0.32.0: google-genai 已移除 —— 辅助层(网感二审/结构审/识图/截图分析)
# 从 Gemini 迁到 Kimi,走 anthropic SDK 打 Moonshot 的 anthropic-compat
# 端点,不再需要 Google 的 SDK。参考帖抓取迁到 SocialDataX。
supabase>=2.4.0,<3.0.0
pydantic>=2.6.0,<3.0.0
PyPDF2>=3.0.0,<4.0.0
python-docx>=1.0.0,<2.0.0
# OpenAI-compatible SDK — v0.32.0 起默认配置里没有 gpt-* 阶段了(门下省/
# 兵部改走 deepseek-v4-flash),所以这个包在默认路径上用不到。保留是因为
# _call_openai_chat 那条 backend 还在:谁想把某个 stage override 回 GPT、
# 或者接一个只讲 OpenAI 协议的中转,不用改 requirements 就能跑。
openai>=1.40.0,<2.0.0
# MCP SDK — used by the trend scout to talk to SocialDataX's first-party
# MCP servers (streamable-HTTP) for real 小红书 content. Pure Python, so no
# Node runtime is needed in the deploy. See pipeline/agents/socialdatax_client.py.
# Floor at 1.9.4: earlier 1.9.x required datetime.timedelta timeouts (we pass
# timedelta anyway, belt-and-braces) and had streamable-HTTP fixes.
mcp>=1.9.4,<2.0.0
